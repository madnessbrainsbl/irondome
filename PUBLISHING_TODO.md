# Publishing TODO

Перед публикацией на GitHub нужно:

1. Проверить, что нигде не осталось реальных IP/VPS/bridge-значений.
2. Заменить все machine-specific пути на шаблоны вида `__PROJECT_ROOT__`.
3. Убедиться, что нигде нет email, refresh/access token и auth-state.
4. Подготовить один canonical install flow.
5. Сделать отдельный `README` для public-аудитории.
6. Добавить smoke test:
   `tor -> 1080 -> 8119 -> 8317 -> strict route -> stop`
7. Решить, какая лицензия будет использоваться.
8. Отдельно описать limitations и threat model.

Минимальный безопасный public-репозиторий должен состоять из:

- шаблонов
- инструкций
- скриптов-генераторов
- без живой боевой конфигурации и без пользовательских секретов
